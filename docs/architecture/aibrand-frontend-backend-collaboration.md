# AiBrand 前后端协作关系图谱

> 最后更新: 2026-06-09 | 渠道中心 V1.0 + 账号绑定闭环 + 评论互动模块

---

## 一、核心协作全景图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          前后端协作全景                                       │
│                                                                             │
│  ┌──────────────────────────┐        ┌──────────────────────────────────┐  │
│  │       前端 (Next.js)       │        │          后端 (NestJS)            │  │
│  │        :3099              │        │           :8080                  │  │
│  │                           │  HTTP  │                                  │  │
│  │  ┌─────────────────────┐  │  JWT   │  ┌────────────────────────────┐  │  │
│  │  │ Page Component      │──┼───────▶│  │ Controller                 │  │  │
│  │  │ (React State)       │  │       │  │ (Route + Zod Validation)    │  │  │
│  │  └────────┬────────────┘  │       │  └────────────┬───────────────┘  │  │
│  │           │               │       │               │                  │  │
│  │           ▼               │       │               ▼                  │  │
│  │  ┌─────────────────────┐  │       │  ┌────────────────────────────┐  │  │
│  │  │ BFF Route Handler   │──┼───────▶│  │ Service                    │  │  │
│  │  │ (聚合/代理/本地存储)  │  │       │  │ (Business Logic)           │  │  │
│  │  └────────┬────────────┘  │       │  └────────────┬───────────────┘  │  │
│  │           │               │       │               │                  │  │
│  │           ▼               │       │               ▼                  │  │
│  │  ┌─────────────────────┐  │       │  ┌────────────────────────────┐  │  │
│  │  │ API Client           │  │       │  │ Repository                 │  │  │
│  │  │ (engagementApi etc.) │  │       │  │ (Mongoose / MongoDB)       │  │  │
│  │  └─────────────────────┘  │       │  └────────────┬───────────────┘  │  │
│  │                           │       │               │                  │  │
│  └──────────────────────────┘        │               ▼                  │  │
│                                      │  ┌────────────────────────────┐  │  │
│  ┌──────────────────────────┐        │  │  MongoDB                    │  │  │
│  │   外部集成                 │        │  │  user_third_account        │  │  │
│  │                           │        │  │  content_stat              │  │  │
│  │  ┌─────────────────────┐  │        │  │  sys_user                  │  │  │
│  │  │ Dify AI (:5001)     │  │        │  └────────────────────────────┘  │  │
│  │  │ n8n (:5678)         │  │        │                                  │  │
│  │  │ Redis (:6379)       │  │        └──────────────────────────────────┘  │
│  │  │ Extension (Browser) │  │                                              │
│  │  └─────────────────────┘  │                                              │
│  └──────────────────────────┘                                               │
│                                                                             │
│  ════════════════════ 协作模式 ════════════════════                          │
│                                                                             │
│  模式1: BFF 代理转发                                                        │
│    前端 → BFF → NestJS → MongoDB → 返回                                     │
│    使用场景: 账号 CRUD、数据查询                                              │
│                                                                             │
│  模式2: BFF 直接处理 + 本地存储                                              │
│    前端 → BFF → 本地 Map/JSON → 返回                                        │
│    使用场景: Mock 数据、不在后端模块中的功能                                  │
│                                                                             │
│  模式3: 前端直调外部服务                                                      │
│    前端 → Extension → 第三方平台 API → 本地 → BFF sync                       │
│    使用场景: 平台账号检测 (Extension 暂停中)                                  │
│                                                                             │
│  模式4: 外部服务 → 后端 → BFF → 前端                                         │
│    n8n Webhook → NestJS → BFF 聚合 → 前端 SSE/轮询                          │
│    使用场景: AI 对话流、数据更新推送                                          │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 二、模块级协作映射

### 2.1 平台账号绑定

```
前端                                后端
────                                ────

channels/page.tsx                   Next.js BFF                         NestJS                          MongoDB
─────────────────                   ──────────                         ──────                          ───────

┌──────────────┐                    ┌──────────────────────┐           ┌──────────────────┐           ┌─────────────────────┐
│ 平台账号 Tab  │                    │ api/channels/accounts│           │ AccountController │           │ user_third_account   │
│              │                    │                      │           │                  │           │                     │
│ useEffect    │─── GET ───────────▶│ GET handler          │─── GET ──▶│ GET list/all     │─── find ─▶│ {                   │
│ fetchAccounts│                    │                      │           │                  │           │   type: "douyin",    │
│              │◀── { data: [...] }─│ ← normalizeBackend   │◀─ [...] ─│ ← getUserAccounts│◀─ docs ──│   account: "梦想...",│
│              │                    │                      │           │                  │           │   fansCount: 21      │
│ setAccounts  │                    │                      │           │                  │           │ }                   │
└──────┬───────┘                    └──────────────────────┘           └──────────────────┘           └─────────────────────┘
       │
       │ 用户点击「绑定」
       ▼
┌──────────────┐                    ┌──────────────────────┐           ┌──────────────────┐           ┌─────────────────────┐
│ BindModal    │                    │ api/channels/accounts│           │ AccountController │           │ user_third_account   │
│              │                    │                      │           │                  │           │                     │
│ 输入用户名    │─── POST ──────────▶│ POST handler         │─── POST ─▶│ POST login       │─── upsert▶│ +1 document         │
│ 点击确认      │                    │                      │           │                  │           │                     │
│              │◀── { data: acc }───│ ← normalizeBackend   │◀─ {acc}──│ ← addAccount     │◀─ saved──│                     │
│              │                    │                      │           │                  │           │                     │
│ onBind()     │                    │                      │           │                  │           │                     │
└──────────────┘                    └──────────────────────┘           └──────────────────┘           └─────────────────────┘
```

### 2.2 内容发布

```
前端                                后端
────                                ────

channels/page.tsx                   Next.js BFF                         任务队列                        Extension (暂停)
─────────────────                   ──────────                         ──────                        ────────────────

┌──────────────┐                    ┌──────────────────────┐           ┌──────────────────┐
│ 一键分发 Tab  │                    │ api/publish/agent    │           │ pushPublishTask  │
│              │                    │                      │           │                  │
│ 编辑内容      │─── POST ──────────▶│ POST handler         │─── push ─▶│ pendingTasks[]   │
│ 质检确认      │                    │                      │           │ [taskId, url,    │
│              │◀── { taskId }──────│ ← 生成 taskId        │◀─ queued─│  createdAt]      │
│              │                    │                      │           └────────┬─────────┘
│ 显示反馈      │                    │                      │                    │
└──────────────┘                    └──────────────────────┘           ┌────────▼─────────┐
                                                                      │ Extension Ping    │
                                                                      │ GET /api/extension│
                                                                      │ /ping             │
                                                                      │ ← NEW_TASK        │
                                                                      │ ← 打开发布 URL     │
                                                                      └──────────────────┘
```

### 2.3 评论互动

```
前端                                后端
────                                ────

CommentsTimeline                    Next.js BFF                         数据源                          AI 引擎
────────────────                    ──────────                         ────                          ──────

┌──────────────┐                    ┌──────────────────────┐           ┌──────────────────┐
│ 评论列表      │                    │ api/channels/comments│           │ comments-mock.ts │
│              │─── GET ───────────▶│ GET handler          │─── read ─▶│ generateMock     │
│ (4维筛选)     │                    │                      │           │ Comments()       │
│              │◀── { comments }────│ ← filterComments     │◀─ data ──│ 35 comments      │
└──────┬───────┘                    └──────────────────────┘           └──────────────────┘
       │
       │ 点击「回复」
       ▼
┌──────────────┐                    ┌──────────────────────┐           ┌──────────────────┐        ┌──────────────────┐
│ ReplyPipeline│                    │ api/channels/comments│           │ REPLY_TEMPLATES  │        │ Dify RAG (远期)   │
│              │                    │ /reply               │           │                  │        │                  │
│ AI生成建议   │─── POST ──────────▶│ POST handler         │─── match ─▶│ 4 sentiments ×   │   or   │ POST /v1/chat-   │
│              │                    │                      │           │ 4 tones          │────────▶│ messages          │
│              │◀── { suggestions }─│ ← generateAIReplies  │◀─ options─│ = 48 templates   │        │ (SSE stream)     │
│              │                    │                      │           └──────────────────┘        └──────────────────┘
│ 预览编辑     │                    │                      │
│ 确认发送     │─── POST ──────────▶│ /send                │
│              │◀── { results }─────│ ← 逐条模拟发送结果     │
│              │                    │                      │
│ 结果反馈     │                    │                      │
└──────────────┘                    └──────────────────────┘
```

### 2.4 自动回复规则

```
前端                                后端
────                                ────

channels/page.tsx                   Next.js BFF                         In-Memory Store
(engView='rules')                   ──────────                         ──────────────

┌──────────────┐                    ┌──────────────────────┐           ┌──────────────────┐
│ 规则列表      │                    │ api/channels/comments│           │ _rules[]         │
│              │─── GET ───────────▶│ /rules                │─── read ─▶│ 4 预置规则        │
│              │◀── { rules }───────│ GET handler           │◀─ array──│                  │
│              │                    │                      │           └──────────────────┘
│ 开关切换      │─── POST ──────────▶│ /rules/toggle         │─── update─▶│ enabled: true/false│
│              │◀── { rule }────────│ POST handler          │◀─ updated─│                  │
│              │                    │                      │           └──────────────────┘
│ 删除规则      │─── DELETE ────────▶│ /rules?id=xxx        │─── splice─▶│ updated          │
│              │◀── { ok: true }────│ DELETE handler        │◀─ removed─│                  │
└──────────────┘                    └──────────────────────┘           └──────────────────┘
```

---

## 三、完整请求路径速查表

```
┌──────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│                              每个前端操作 → 完整后端调用链                                                   │
├──────────────────────────┬────────────────┬──────────────────┬──────────────────┬────────────────────────┤
│ 前端操作                  │ BFF (Next.js)   │ NestJS Controller │ MongoDB           │ 外部服务               │
├──────────────────────────┼────────────────┼──────────────────┼──────────────────┼────────────────────────┤
│                          │                │                  │                  │                        │
│ 加载平台账号列表           │ GET /channels/ │ GET /account/    │ user_third_       │ —                      │
│                          │ accounts       │ list/all         │ account.find()    │                        │
│                          │                │                  │                  │                        │
│ 手动绑定平台账号           │ POST /channels/│ POST /account/   │ user_third_       │ —                      │
│                          │ accounts       │ login            │ account.upsert()  │                        │
│                          │                │                  │                  │                        │
│ 解绑平台账号              │ DELETE         │ POST /account/   │ user_third_       │ —                      │
│                          │ /channels/     │ delete/:id       │ account.delete()  │                        │
│                          │ accounts?id=   │                  │                  │                        │
│                          │                │                  │                  │                        │
│ 一键发布                  │ POST /api/     │ — (BFF 直接处理)  │ —                │ pendingTasks[] →       │
│                          │ publish/agent  │                  │                  │ Extension (暂不可用)    │
│                          │                │                  │                  │                        │
│ 获取评论列表              │ GET /channels/ │ — (Mock 引擎)     │ —                │ —                      │
│                          │ comments       │                  │                  │                        │
│                          │                │                  │                  │                        │
│ AI 生成回复建议           │ POST /channels/│ — (模板库模拟)    │ —                │ Dify RAG (远期)         │
│                          │ comments/reply │                  │                  │ POST /v1/chat-messages  │
│                          │                │                  │                  │                        │
│ 批量发送回复              │ POST /channels/│ — (模拟)          │ —                │ 平台回复 API (远期)     │
│                          │ comments/send  │                  │                  │                        │
│                          │                │                  │                  │                        │
│ 获取/创建/删除规则         │ GET/POST/DEL   │ — (BFF 内存)      │ —                │ —                      │
│                          │ /channels/     │                  │                  │                        │
│                          │ comments/rules │                  │                  │                        │
│                          │                │                  │                  │                        │
│ Extension 注册/轮询       │ GET/POST /api/ │ — (BFF 内存)      │ —                │ Chrome Extension        │
│                          │ extension/ping │                  │                  │ window.AiBrandPlugin    │
│                          │                │                  │                  │                        │
│ GEO 内容评分              │ POST /api/geo/ │ — (BFF 直接处理)  │ —                │ —                      │
│                          │ score          │                  │                  │                        │
│                          │                │                  │                  │                        │
│ Agent 列表                │ GET /api/      │ — (BFF Mock)     │ —                │ —                      │
│                          │ agents/list    │                  │                  │                        │
│                          │                │                  │                  │                        │
│ Dashboard 聚合            │ GET /api/      │ GET /account/    │ user_third_       │ —                      │
│                          │ dashboard      │ statistics       │ account.aggregate │                        │
│                          │                │                  │                  │                        │
│ 用户登录                  │ —              │ POST /login/     │ sys_user          │ Redis 验证码存储        │
│                          │                │ mail/verify      │                  │                        │
│                          │                │                  │                  │                        │
│ AI Agent 对话 (SSE)       │ — (直调后端)    │ POST /agent/chat │ —                │ Dify POST /v1/          │
│                          │                │ (SSE 流)          │                  │ chat-messages           │
└──────────────────────────┴────────────────┴──────────────────┴──────────────────┴────────────────────────┘
```

---

## 四、数据流向图 (按 Tab)

```
channels 页面 4 个 Tab 的数据流向:

┌─────────────────────────────────────────────────────────────────────────────┐
│ Tab: 平台账号                                                                │
│                                                                             │
│  ┌─────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐            │
│  │ 页面加载  │────▶│ doScan() │────▶│fetchAcc()│────▶│setAccounts│            │
│  │         │     │          │     │          │     │  (State) │            │
│  │         │     │Extension?│     │GET BFF   │     │          │            │
│  │         │     │  Yes→检测 │     │← 账号列表 │     │ 渲染卡片  │            │
│  │         │     │  No→跳过  │     │          │     │          │            │
│  └─────────┘     └──────────┘     └──────────┘     └──────────┘            │
│                                                                             │
│  ┌─────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐            │
│  │ 点击绑定  │────▶│BindModal │────▶│POST BFF  │────▶│MongoDB   │            │
│  │         │     │ 填用户名  │     │bind acc  │     │持久化    │            │
│  │         │     │          │     │          │     │          │            │
│  └─────────┘     └──────────┘     └──────────┘     └──────────┘            │
│                                                                             │
│  ┌─────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐            │
│  │ 点击解绑  │────▶│handleUnb│────▶│DELETE BFF│────▶│DELETE    │            │
│  │         │     │ind(p.id) │     │unbind    │     │MongoDB   │            │
│  └─────────┘     └──────────┘     └──────────┘     └──────────┘            │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ Tab: 数据分析                                                                │
│                                                                             │
│  ┌─────────┐     ┌──────────┐     ┌──────────┐                             │
│  │ 页面渲染  │────▶│accList   │────▶│ 计算指标  │  纯前端计算，无需 API 调用     │
│  │         │     │(共享状态) │     │粉丝/作品  │  (数据来自平台账号 Tab 的      │
│  │         │     │          │     │          │   accounts state)            │
│  └─────────┘     └──────────┘     └──────────┘                             │
│                                                                             │
│  远期: GET /api/channels/accounts → 包含趋势数据 → ECharts 渲染               │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ Tab: 一键分发                                                                │
│                                                                             │
│  ┌─────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐            │
│  │ 编辑内容  │────▶│质量审查   │────▶│确认发送   │────▶│POST      │            │
│  │ 选平台   │     │4维评分   │     │          │     │/publish  │            │
│  │         │     │(前端本地) │     │          │     │/agent    │            │
│  └─────────┘     └──────────┘     └──────────┘     └────┬─────┘            │
│                                                        │                   │
│                                              ┌─────────▼──────────┐        │
│                                              │ pushPublishTask()  │        │
│                                              │ in-memory queue    │        │
│                                              │ → Extension 消费    │        │
│                                              │ (当前不可用)        │        │
│                                              └────────────────────┘        │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ Tab: 评论互动                                                                │
│                                                                             │
│  ┌─────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐            │
│  │ 评论列表  │────▶│GET BFF   │────▶│Mock引擎  │────▶│筛选/排序  │            │
│  │ (筛选)   │     │/comments │     │生成数据   │     │渲染时间线 │            │
│  └────┬────┘     └──────────┘     └──────────┘     └──────────┘            │
│       │                                                                    │
│       │ 单条回复 / 批量回复                                                  │
│       ▼                                                                    │
│  ┌─────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐            │
│  │Reply    │────▶│POST BFF  │────▶│模板引擎   │────▶│逐条确认   │            │
│  │Pipeline │     │/reply    │     │生成建议   │     │发送       │            │
│  │5阶段    │     │          │     │          │     │          │            │
│  └─────────┘     └──────────┘     └──────────┘     └──────────┘            │
│                                                                             │
│  ┌─────────┐     ┌──────────┐     ┌──────────┐                             │
│  │ 规则管理  │────▶│GET/POST  │────▶│内存规则表 │  规则引擎:                  │
│  │         │     │/rules    │     │4条预置   │  关键词 → 匹配 → 回复         │
│  └─────────┘     └──────────┘     └──────────┘                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 五、状态共享 & 数据依赖关系

```
channels/page.tsx 内部状态依赖图:

                        ┌─────────────┐
                        │   accounts   │  ← 共享状态 (所有 Tab 共用)
                        │   (State)    │
                        └──────┬──────┘
                               │
          ┌────────────────────┼────────────────────┐
          │                    │                    │
          ▼                    ▼                    ▼
   ┌──────────────┐    ┌──────────────┐    ┌──────────────┐
   │ 平台账号 Tab  │    │ 数据分析 Tab  │    │ 一键分发 Tab  │
   │              │    │              │    │              │
   │ 读取: accounts│    │ 读取: accounts│    │ 写入: 无     │
   │ 写入: bind/  │    │ 只读         │    │ 读取: accounts│
   │      unbind  │    │ accList计算  │    │ 判断绑定状态  │
   │              │    │ 图表渲染     │    │ 平台选择器    │
   └──────────────┘    └──────────────┘    └──────────────┘
          │
          │ 写入
          ▼
   ┌──────────────┐
   │  后端         │
   │  MongoDB     │  ← 持久化
   │  user_third_ │
   │  account     │
   └──────────────┘


评论互动 Tab 独立状态:

   ┌──────────────┐    ┌──────────────┐
   │ 评论列表      │    │ 自动规则      │
   │              │    │              │
   │ comments[]   │    │ rules[]      │
   │ (自管理)     │    │ (自管理)     │
   └──────┬───────┘    └──────┬───────┘
          │                   │
          ▼                   ▼
   ┌──────────────┐    ┌──────────────┐
   │ GET /comments│    │ GET /rules   │
   │ (Mock引擎)   │    │ (BFF内存)    │
   └──────────────┘    └──────────────┘

   数据流向: 评论互动 Tab 和其他 3 个 Tab 无状态共享 (独立数据源)
```

---

## 六、错误处理协作链

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                       错误处理 — 分层捕获                                      │
│                                                                             │
│  前端 (React)                    BFF (Next.js)               NestJS 后端     │
│  ────────────                    ─────────────               ──────────      │
│                                                                             │
│  try {                          try {                       try {           │
│    await api.call()               fetch(backend)              db.query()    │
│  } catch (e) {        ──▶       } catch (e) {     ──▶      } catch (e) {   │
│    setError(e)        ◀──         return 500       ◀──        throw         │
│    → ErrorFallback                 { error }                   AppException │
│    → 重试按钮                                                               │
│  }                              }                           }              │
│                                                                             │
│  状态覆盖:                                                                    │
│  ┌──────────┬──────────┬──────────┬──────────┐                              │
│  │ Loading  │  Empty   │  Error   │  Normal  │                              │
│  │ Spinner  │ 引导文案  │ 重试按钮  │ 数据展示  │                              │
│  └──────────┴──────────┴──────────┴──────────┘                              │
│                                                                             │
│  关键位置:                                                                    │
│  · channels/page.tsx: fetchAccounts() catch → setLoading(false)             │
│  · channels/page.tsx: doScan() → 扩展通信失败 → 显示警告                      │
│  · comments/*/route.ts: try/catch → return Response.json({ error })         │
│  · BindModal: API 调用失败 → setMsg({ type: 'error' })                      │
│  · doPublish: fetch 异常 → setMsg + 标记所有平台失败                          │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 七、前后端部署 & 通信架构

```
                          ┌───────────────────────────────────────────┐
                          │              用户浏览器                     │
                          │                                           │
                          │  ┌─────────────────────────────────────┐  │
                          │  │ Chrome + AiBrand Extension (暂停)    │  │
                          │  │ window.AiBrandPlugin                │  │
                          │  │ Content Script → 平台 API 检测       │  │
                          │  └─────────────────────────────────────┘  │
                          │                                           │
                          │  http://localhost:3099/channels           │
                          │  ┌─────────────────────────────────────┐  │
                          │  │ Next.js Dev Server (BFF)            │  │
                          │  │   ├── Page Components              │  │
                          │  │   ├── Route Handlers (API Routes)   │  │
                          │  │   └── API Clients                  │  │
                          │  └──────────────┬──────────────────────┘  │
                          └─────────────────┼─────────────────────────┘
                                            │
                          Proxy :6060 (C:\Users\XIAOMI\aibrand-proxy.js)
                          Extension API 代理
                                            │
                          ┌─────────────────┼─────────────────────────┐
                          │                 │   Docker Network        │
                          │                 │   aibrand-network       │
                          │                 ▼                         │
                          │  ┌─────────────────────────────────────┐  │
                          │  │ nginx :8080                         │  │
                          │  │   反向代理 · 限流 · HTTPS · CSP      │  │
                          │  └──────────────┬──────────────────────┘  │
                          │                 │                         │
                          │       ┌─────────┼─────────┐              │
                          │       ▼         ▼         ▼              │
                          │  ┌─────────┐┌─────────┐┌─────────┐     │
                          │  │NestJS   ││Next.js  ││AI 服务   │     │
                          │  │:3002    ││:3000    ││:3010    │     │
                          │  └────┬────┘└─────────┘└─────────┘     │
                          │       │                                  │
                          │       ▼                                  │
                          │  ┌─────────┐┌─────────┐┌─────────┐     │
                          │  │MongoDB  ││ Redis   ││ Dify    │     │
                          │  │:27017   ││ :6379   ││ :5001   │     │
                          │  └─────────┘└─────────┘└─────────┘     │
                          │                                         │
                          └─────────────────────────────────────────┘

通信协议:
  浏览器 ↔ Next.js (:3099)      HTTP/1.1 + JWT Bearer
  Next.js ↔ NestJS (:8080)      HTTP/1.1 + JWT Bearer (通过 nginx)
  NestJS ↔ MongoDB (:27017)     MongoDB Wire Protocol
  NestJS ↔ Dify (:5001)         HTTP/1.1 + API Key
  Extension ↔ Proxy (:6060)     HTTP/1.1 + JWT Bearer
  Extension ↔ 平台 API           HTTPS + Cookie (直接调用)
```

---

## 八、当前实现状态速查

```
┌─────────────────────────────────────────────────────────────────┐
│  模块          前端              BFF            后端           │
│─────────────────────────────────────────────────────────────────│
│  账号绑定       ✅ BindModal     ✅ route.ts    ✅ NestJS      │
│                + AccountCard    (代理+本地)    Account Module │
│                                                               │
│  账号列表       ✅ fetchAccounts ✅ GET handler  ✅ MongoDB     │
│                自动加载         合并双源数据     持久化         │
│                                                               │
│  数据分析       ✅ 柱状图+表格    —               —             │
│                纯前端计算                                       │
│                                                               │
│  内容发布       ✅ 5阶段管线     ✅ route.ts    ⚠️ 任务队列     │
│                质检+确认+发送    pushPublish    Extension未运行 │
│                                                               │
│  评论列表       ✅ Timeline      ✅ route.ts    — (Mock)       │
│                4维筛选+批量      Mock引擎                       │
│                                                               │
│  AI 回复        ✅ Pipeline      ✅ /reply      — (模板库)      │
│                5阶段            模板匹配        远期: Dify RAG  │
│                                                               │
│  回复规则       ✅ RuleCard      ✅ /rules      — (BFF内存)     │
│                开关+删除         CRUD                          │
│                                                               │
│  评论统计       ✅ 数字卡片       ✅ /stats      — (Mock)       │
│                4指标展示        实时计算                        │
└─────────────────────────────────────────────────────────────────┘

图例:
  ✅ = 完整可用 (前后端打通)
  ⚠️ = 部分可用 (缺下游消费)
  —  = 当前为 Mock / 不需要
  远期 = 待接入
```
